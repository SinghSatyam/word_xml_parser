== README

Steps to run:

* Clone the project

* Change your present working directory to the project directory

* Run 'bundle install'

* Run 'rails s'

* Open browser and type 'localhost:3000'

* You should see the parsed XML